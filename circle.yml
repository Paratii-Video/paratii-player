dependencies:
  pre:
    - curl https://install.meteor.com | /bin/sh
test:
  override:
    - TEST_BROWSER_DRIVER=nightmare NODE_OPTIONS='--harmony' meteor test --once --driver-package dispatch:mocha  --port 9999

deployment:
  staging:
    branch: master
    commands:
      - ./deploy_staging.sh
